package com.invado.crm.repository;

import com.invado.crm.domain.TerritoryType;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 * Spring Data JPA repository for the TerritoryType entity.
 */
public interface TerritoryTypeRepository extends JpaRepository<TerritoryType,Long>{

}
