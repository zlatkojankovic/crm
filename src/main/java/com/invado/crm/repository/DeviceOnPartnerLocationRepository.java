package com.invado.crm.repository;

import com.invado.crm.domain.DeviceOnPartnerLocation;
import org.springframework.data.jpa.repository.JpaRepository;

/**
 * Spring Data JPA repository for the DeviceOnPartnerLocation entity.
 */
public interface DeviceOnPartnerLocationRepository extends JpaRepository<DeviceOnPartnerLocation,Long>{

}
